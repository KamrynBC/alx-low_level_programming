#include "main.h"

/**
 * _strspn - entry point
 * @s: first string
 * @accpet: second string
 *
 * Return: pointer to s. null if otherwise
 */
unsigned int _strspn(char *s, char *accept)
{
	return (s);
}
